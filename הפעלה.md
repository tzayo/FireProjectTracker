# ▶️ הפעלת המערכת - הוראות פשוטות

---

## 🎯 שתי דרכים להפעלה

---

## אפשרות 1: הפעלה אוטומטית (מומלץ) 🚀

### פשוט הרץ:

```bash
./start-system.sh
```

**זהו!** המערכת תעלה אוטומטית.

**אם Script לא עובד:**
```bash
chmod +x start-system.sh
./start-system.sh
```

---

## אפשרות 2: הפעלה ידנית 🔧

### פתח 2 חלונות Terminal

#### Terminal 1 - Backend (שרת):

```bash
cd backend
python app.py
```

או עם Python 3:
```bash
cd backend
python3 app.py
```

**תראה:**
```
* Running on http://127.0.0.1:5000
* Running on http://0.0.0.0:5000
```

✅ אם רואה את זה - Backend עלה!

---

#### Terminal 2 - Frontend (ממשק):

```bash
cd frontend
npm start
```

**תראה:**
```
Compiled successfully!

You can now view fire-department-tracker in the browser.

  Local:            http://localhost:3000
```

✅ הדפדפן אמור להיפתח אוטומטית!

---

## 🌐 איפה לגשת?

כשהכל רץ:

- **ממשק המשתמש:** http://localhost:3000
- **API Server:** http://localhost:5000

פתח דפדפן וגש ל: **http://localhost:3000**

---

## ⚠️ בעיות נפוצות

### Backend לא עולה

**שגיאה:** `ModuleNotFoundError: No module named 'flask'`

**פתרון:**
```bash
cd backend
pip install Flask flask-cors flask-sqlalchemy
# או
pip3 install Flask flask-cors flask-sqlalchemy
```

---

**שגיאה:** `Address already in use` או `Port 5000 is already in use`

**פתרון 1:** הרוג תהליך על פורט 5000:
```bash
# Mac/Linux:
lsof -ti:5000 | xargs kill -9

# Windows (PowerShell):
Get-Process -Id (Get-NetTCPConnection -LocalPort 5000).OwningProcess | Stop-Process
```

**פתרון 2:** שנה פורט ב-`backend/app.py`:
```python
# שורה אחרונה, שנה מ-5000 ל-5001:
app.run(debug=True, host='0.0.0.0', port=5001)
```

---

### Frontend לא עולה

**שגיאה:** `npm: command not found`

**פתרון:** התקן Node.js מ-https://nodejs.org

---

**שגיאה:** Module errors

**פתרון:** נקה והתקן מחדש:
```bash
cd frontend
rm -rf node_modules
npm install
npm start
```

---

**שגיאה:** `Port 3000 is already in use`

**פתרון:**
```bash
# הרוג תהליך על פורט 3000
lsof -ti:3000 | xargs kill -9
```

או הרץ על פורט אחר:
```bash
PORT=3001 npm start
```

---

## 🛑 עצירת המערכת

### אפשרות 1: סקריפט

```bash
./stop-system.sh
```

### אפשרות 2: ידנית

בכל Terminal לחץ: `Ctrl + C`

או:
```bash
# הרוג את כל התהליכים
pkill -f "python.*app.py"
pkill -f "react-scripts"
```

---

## ✅ איך לדעת שהכל עובד?

### Backend עובד אם:
```bash
curl http://localhost:5000/api/dashboard/stats
```
מחזיר JSON עם נתונים

### Frontend עובד אם:
- דפדפן נפתח ל-http://localhost:3000
- רואה את לוח הבקרה
- רואה תפריט עליון בעברית

---

## 🎯 בדיקה מהירה

לאחר הפעלה, בדוק:

1. ✅ פתח http://localhost:3000
2. ✅ רואה "לוח בקרה - סקירה כללית"
3. ✅ רואה תפריט עליון עם 8 כפתורים
4. ✅ לחץ "הידרנטים" - העמוד נטען
5. ✅ לחץ "+ הוסף הידרנט" - טופס נפתח
6. ✅ סגור את הטופס

**אם כל אלה עובדים →** המערכת פועלת מצוין! 🎉

---

## 🚨 עזרה דחופה

אם שום דבר לא עובד:

1. **בדוק שיש לך Python:**
   ```bash
   python --version
   # או
   python3 --version
   ```
   צריך לראות: `Python 3.8` ומעלה

2. **בדוק שיש לך Node.js:**
   ```bash
   node --version
   ```
   צריך לראות: `v18` ומעלה

3. **התקן חסרים:**
   - Python: https://www.python.org/downloads/
   - Node.js: https://nodejs.org/

4. **התקן תלויות מחדש:**
   ```bash
   cd backend
   pip install -r requirements.txt
   
   cd ../frontend
   npm install
   ```

5. **נסה שוב:**
   ```bash
   ./start-system.sh
   ```

---

## 📞 עדיין לא עובד?

קרא את פתרון הבעיות ב:
- **QUICKSTART.md** - פתרון בעיות התקנה
- **SYSTEM_GUIDE.md** - פתרון בעיות טכניות

או פנה למנהל המערכת.

---

## 🎉 הצלחה!

ברגע שהמערכת עלתה:

1. 📖 קרא [מדריך_מהיר_עברית.md](מדריך_מהיר_עברית.md)
2. 🎯 התחל להוסיף נתונים
3. 🚀 תהנה מהמערכת!

---

**גרסה:** 2.0  
**עדכון:** 2025-10-10  
**רמת קושי:** 🟢 קל
